# HANDOVER.md — チャット引き継ぎファイル

> **運用方法**
> - チャット開始時：このファイルを読み込ませて文脈を引き継ぐ
> - チャット終了時：AIに「HANDOVER.md を更新して」と依頼し、進捗を記録する
> - 新しいチャットでスムーズに続行できるようにする

---

## 1. プロジェクト概要

| 項目 | 内容 |
|:---|:---|
| **プロジェクト名** | xiao-scanner（XIAO環境スキャナーシステム） |
| **GitHub** | https://github.com/kakinymax/xiao-scanner.git |
| **ブランチ** | `main`（直接プッシュ運用） |
| **ワークスペース** | `/Users/ejentoyou/mac移行データ/antigravity/scratch/2_xiao_env_scanner_system` |

### システム概要
XIAO（Seeed Studio製マイコン）で温度・湿度を計測し、不快指数（DI）を算出。BLE経由でWebブラウザにデータ転送し、リアルタイムグラフ表示・AI予測を行う環境モニタリングシステム。

---

## 2. プロジェクト構成

```
xiao-scanner/
├── index.html              ← Webアプリ本体（約43KB、全機能ここに集約）
├── web_scanner/
│   ├── README.md
│   └── archive/            ← 過去バージョン
├── mcu_firmware/
│   ├── xiao_env_monitor/   ← メインファームウェア（.ino + QRコードライブラリ）
│   ├── xiao_env_ai_collector/ ← AI学習用データ収集ファームウェア
│   └── xiao_env_monitor_pre_ai/ ← AI導入前の旧バージョン
├── ai_training/
│   ├── Colab_Training_Guide.md ← Google Colabでの学習手順
│   ├── parse_bin.py         ← .binデータのパーサー
│   └── logs/
├── tests/
│   ├── initBlocks.test.js   ← テスト（Jest）
│   └── benchmarks/
├── archives/                ← ファームウェアバックアップ
├── .jules/
│   ├── sentinel.md          ← セキュリティ学習ログ
│   └── palette.md           ← UX学習ログ
├── ISSUES.md                ← 未解決課題（解決後は削除する運用）
├── README.md                ← プロジェクト説明 + 更新履歴
├── HANDOVER.md              ← ★ このファイル
├── package.json
├── jest.config.js
└── .gitignore
```

---

## 3. 現在の状態

### Gitの状態
- **最新コミット**: `a235cba` — セキュリティとパフォーマンスの改善：innerHTMLのXSS脆弱性対応およびCanvas描画の最適化
- **作業ツリー**: クリーン（未コミットの変更なし）
- **リモート同期**: 最新がプッシュ済み

### 環境に関する注意事項
- `npm` コマンドはパスが通っていない可能性あり（テスト実行不可の場合がある）
- 自動化スクリプトやフレームワークの新規作成は行わない方針

---

## 4. 未解決の課題（⚠️ 最優先）

### AI予測用テキスト生成時の温度異常バグ（ISSUES.md）
**症状**: 温度が「60度〜70度台」というあり得ない数値になる

**原因（3つ）**:
1. マイコン側のAIログデータ（`.bin`）にヘッダーがなく、データの区切りが不明瞭
2. BLE転送時のパケット欠損でオフセットがズレ、湿度の値を温度として誤読
3. ブラウザ側で固定長1440件で区切っており、欠損やズレを考慮していない

**修正方針**:
- マイコン側：ヘッダー導入、データ構造の堅牢化（タイムスタンプ/チェックサム/マジックナンバー）
- HTML側：異常値のサニタイズ処理、パースロジック改修

**完了条件**:
- バグ修正完了後 → `ISSUES.md` を削除
- `README.md` に修正完了の履歴を記録

---

## 5. 完了済み作業ログ

### 2026-05-21（第2弾修正）— 会話ID: `33fc7e72`
外部スキャナーからの計10件の改善提案を検証。2件を実施、8件を見送り。

**実施した修正**:
- ✅ XSS脆弱性対策：サマリーテーブルの `innerHTML` → `textContent` 方式に変更
- ✅ Canvas描画最適化：`fillStyle` にキャッシュガードを導入

**見送った修正**:
- ❌ テスト追加（6件）→ npm環境未構築のため
- ❌ QRコードライブラリ内部改造（2件）→ バグリスクのため

**コミット**: `a235cba`

---

### 2026-05-21（第1弾修正）— 会話ID: 不明（Jules経由の可能性）
- `sprintf` → `snprintf` への安全な書き換え（バッファオーバーフロー対策）
- 配列フラットニング、CSV文字列構築、BLE定数の最適化

**コミット**: `7d40d5e`

---

### 2026-05-20
- CDN外部スクリプトにSRI（Subresource Integrity）を導入
- Chart.jsをv4.5.1に固定
- READMEにファイル構成の説明を追加

---

### それ以前の主要な修正
- `window.open` に `noopener,noreferrer` を追加（リバースタブナビング対策）
- `innerHTML` → DOM API への全体的な置き換え
- Canvasサイズ計算の最適化（映像サイズ変更時のみ実行）

---

## 6. 運用ルール

| ルール | 詳細 |
|:---|:---|
| **スクリプト作成禁止** | 自動化スクリプトやフレームワークの新規作成は行わず、既存ツールのみで対応 |
| **説明は平易に** | 非エンジニアにもわかる日本語で説明する |
| **Git運用** | 修正完了後は `main` ブランチに直接コミット＆プッシュ |
| **ISSUES.md運用** | 課題が解決したら削除し、README.mdに履歴を記録 |
| **セキュリティログ** | `.jules/sentinel.md` に脆弱性の学び・対策を記録 |

---

## 7. 次にやるべきこと

1. **ISSUES.md のバグ修正**（温度異常バグ）— マイコン側＋HTML側の両方
2. ユーザーから新たな修正提案画像が提示された場合の対応

---

*最終更新: 2026-05-21 22:09 JST*
