# プロジェクト課題および修正方針 (ISSUES)

## 📌 現在の課題
**AI予測用のテキスト生成時、温度が「60度〜70度台」というあり得ない数値になるバグ**

### 🔍 原因の詳細
1. **データ構造の欠陥（ヘッダーの不在）**
   マイコン（XIAO）側のAIログデータ（`.bin`）には、記録開始日時やデータ区切りを示すメタデータ（ヘッダー）が存在せず、ひたすら「温度(4byte)・湿度(4byte)・不快指数(4byte)」がベタ書きされている。
2. **BLE転送時のパケット欠損によるオフセットのズレ**
   BLE経由でデータを送信する際、パケット落ち等でデータ長が狂うと、ブラウザ側での読み取り開始位置（オフセット）がズレる。これにより、「本来『湿度』である数値を、『温度』として読み込んでしまう」現象が発生している。（※温度60度〜70度は、実は湿度の値である）
3. **ブラウザ側（`index.html`）のパース処理のミス**
   1日のデータを「1440件」という固定長で区切っており、欠損やズレを考慮していない。また、最新のデータから遡って配列に`push`しているため、時系列が逆転してしまっている部分がある。

---

## 🛠️ 今後の修正方針

### 1. マイコン側（ファームウェア / `.ino`）の修正
* **ヘッダーの導入:** ファイルの先頭に、記録開始のタイムスタンプやバージョン情報を含める。
* **データ構造の堅牢化:** 1レコードごとにタイムスタンプやチェックサムを持たせるか、あるいは明確な区切り文字（マジックナンバー等）を入れて、受信側でズレを検知・復旧できるようにする。

### 2. HTML側（フロントエンド / `index.html`）の修正
* **データのサニタイズ処理:** 読み取った温度データが日本の自然環境で明らかに異常（例：50度以上、-20度以下）な場合は、ズレが発生したとみなしてそのパケット・あるいはその日のデータを除外（または補正）する。
* **パースロジックの改修:** データの区切り（オフセット計算）を厳密に見直し、時系列が正しく反映されるように配列の操作（`push` と `unshift` など）を修正する。

---

## 🚨 運用ルール（AIアシスタントへの指示）

**【重要指令】**
上記の「マイコン側」および「HTML側」の両方の修正作業が完全に完了し、バグが解決したことを確認できたら、以下の手順を実行すること。

1. **この `ISSUES.md` ファイルを削除する。**
2. **`README.md` に「AI予測データの温度異常バグを修正し、処理の堅牢化を完了した」旨の進捗・履歴を記録する。**
